## Setup

### Install poetry
`$ pip install poetry`

### Build Docker
`$ docker-compose build`

### Run app
`$ docker-compose run --rm exam_app`

---
### Notes:
#### Logs file
`logs.txt`

#### Processed file
`processsed_data.parquet`

#### Dropped rows
`dropped_data.csv`
